{
  "name": "smamo-lp",
  "version": "0.1.0",
  "private": true,
  "description": "SMAMO LP (static) + Cloudflare Pages Functions for Stripe",
  "scripts": {
    "dev": "wrangler pages dev . --compatibility-date=2026-05-18",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.10",
    "wrangler": "^3.114.17"
  },
  "dependencies": {
    "stripe": "^18.0.0"
  }
}
